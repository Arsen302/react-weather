import React from "react";
import styles from "../Content/СardContent.module.css";

const СardContent = ({ content }) => {
  return (
    <main>
      <div>{content}</div>
    </main>
  );
};

export default СardContent;
